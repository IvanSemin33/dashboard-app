import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  percentContainer: {
    display: "flex",
    alignItems: "center",
    gap: "5px",
  },
  percentUp: {
    color: "#52C41A",
  },
  percentDown: {
    color: "#F5222D",
  },
});
