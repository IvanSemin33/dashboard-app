import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  logo: {
    fontFamily: "Gropled",
    fontWeight: 700,
    fontSize: "24px",
    lineHeight: 1,
    color: "#000000",
  },
  logoImg: {
    marginLeft: "18px",
    marginTop: "19px",
  },
});
